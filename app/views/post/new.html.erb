<h3>New Post</h3>
<%= form_for @post, :url => :new_post do |f| %>
	<%= f.label :title %></br>
	<%= f.text_field :title %></br>

	<%= f.label :body %></br>
	<%= f.text_area :body, :rows => 9 %></br>

	<%= f.submit :save, :class => "button small" %>
<% end %>